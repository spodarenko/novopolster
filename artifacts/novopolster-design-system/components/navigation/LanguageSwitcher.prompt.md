Language dropdown for the site's 4 languages (DE default, EN/UA/IT) — a key differentiator vs. competitors who serve DE only.

```jsx
<LanguageSwitcher value="DE" onChange={(code) => setLang(code)} tone="light" />
```

Use `tone="dark"` inside the ink header/footer.
